# outer

Sibling folder to **`~/ai agents with containers`**. Use this tree for things you want **outside** that vault/repo (deploy clones, scratch apps, another Cursor window, etc.).

Full path: `/home/sha/outer`
