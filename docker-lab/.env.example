# Copy to `.env` if your tooling loads it (never commit real `.env`).
# Compose sets DATABASE_URL inline; this documents the local shape.

DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/docker_lab
